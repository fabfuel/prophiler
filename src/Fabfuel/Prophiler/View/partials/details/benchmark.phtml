<?php
/**
 * @var \Fabfuel\Prophiler\Toolbar\Formatter\BenchmarkFormatterInterface $benchmark
 */
?>
<dl>
    <dt>Duration</dt>
    <dd><?= $benchmark->getDuration() ?></dd>

    <dt>Memory usage</dt>
    <dd><?= $benchmark->getMemoryUsage() ?></dd>

    <?php if ($benchmark->getMetadata()): ?>
        <?php foreach ($benchmark->getMetadata() as $metadataName => $metadata): ?>
            <?php $this->partial('partials/details/data', ['data' => $metadata, 'dataName' => $metadataName, 'json' => in_array($benchmark->getComponent(), ['MongoDB', 'Elasticsearch'])]); ?>
        <?php endforeach; ?>
    <?php endif; ?>
</dl>
